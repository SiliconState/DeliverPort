# Database connection string (Neon PostgreSQL recommended)
DATABASE_URL="postgresql://user:password@host:5432/deliverport?sslmode=require"

# JWT secret — generate a strong random string (min 32 chars)
JWT_SECRET="change-me-to-a-random-secret-at-least-32-chars"

# Server port (default: 3000)
PORT=3000

# Allowed CORS origins (comma-separated)
CORS_ORIGINS="https://siliconstate.github.io,http://localhost:5500"

# Base JSON-RPC endpoint used for invoice reconciliation
BASE_RPC_URL="https://mainnet.base.org"

# Optional lookback window (in blocks) for /api/invoices/:id/reconcile
RECONCILE_LOOKBACK_BLOCKS=120000

# Optional minimum confirmations before a transfer is considered in reconciliation
RECONCILE_MIN_CONFIRMATIONS=1
